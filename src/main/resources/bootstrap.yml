spring:
  application:
    name: message-v1
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: dev
  cache: redis
  redis:
    host: localhost
    port: 6379
    timeOfLife: 60000

management:
  endpoints:
    web:
      exposure:
        include: refresh